## 1. Inventory And Scope

- [x] 1.1 Enumerate the component-local design `README.md` files under `packages/ui/src/components/**` and confirm which folders represent a single component versus a component family.
- [x] 1.2 Identify any existing README patterns that omit newly required Figma details such as layout behavior, fills, strokes, effects, or build notes.

## 2. README Refresh

- [x] 2.1 Update each targeted component README using the current `design-system-component-docs` skill so the docs reflect the expanded Figma-oriented property checklist.
- [x] 2.2 Ensure each refreshed README updates its size, state, anatomy, token, and Figma mapping sections where needed to cover layout, fills, strokes, effects, and explicit gap handling.
- [x] 2.3 Preserve component-local nuances and existing evidence-based notes instead of flattening all READMEs into a generic template.

## 3. Consistency Review

- [x] 3.1 Review a representative set of refreshed READMEs across forms, feedback, overlay, typography, display, and miscellaneous components for consistent structure and completeness.
- [x] 3.2 Fix outlier READMEs that still miss skill-required Figma details or that overstate unsupported behavior.

## 4. Final Validation

- [x] 4.1 Verify the refreshed documentation remains in the existing component `README.md` files and that no parallel design-doc location was introduced.
- [x] 4.2 Confirm the updated READMEs can serve as the new baseline for future skill-driven refreshes without duplicating stale sections.
