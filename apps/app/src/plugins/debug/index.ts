;(() => {
  if (process.env.NODE_ENV === 'development') {
    // debugger
    // window.addEventListener('click', (e) => {
    //   console.log(e, window, (window as any).inspect)
    //   if (e.ctrlKey) {
    //     e.stopPropagation()
    //     e.preventDefault()
    //     ;(globalThis.window as any).inspect(e.target)
    //   }
    // })
  }
})()
