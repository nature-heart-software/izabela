{
  "name": "root",
  "private": true,
  "scripts": {
    "install": "npm run build:deps",
    "bootstrap": "npx lerna bootstrap --no-ci",
    "dev": "npm run bootstrap && npx lerna run serve --scope izabela-next --stream",
    "build": "npm run bootstrap && npx lerna run build",
    "build:deps": "npm run bootstrap && npx lerna run build --scope  '{@packages/*,@apps/app-server}' --stream",
    "build:app": "npm run bootstrap && npx lerna run build --scope '{@packages/*,@apps/app-server,izabela-next}' --stream"
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.0.1",
    "@parcel/transformer-typescript-types": "^2.0.1",
    "lerna": "^5.6.2",
    "microbundle": "^0.15.0",
    "nx": "14.5.10",
    "typescript": "^4.5.4"
  },
  "release": {
    "branches": [
      "main",
      {
        "name": "pre/rc",
        "channel": "pre/rc",
        "prerelease": "rc"
      },
      {
        "name": "beta",
        "channel": "beta",
        "prerelease": "beta"
      },
      {
        "name": "alpha",
        "channel": "alpha",
        "prerelease": "alpha"
      }
    ],
    "plugins": [
      [
        "@semantic-release/npm",
        {
          "npmPublish": false
        }
      ]
    ]
  },
  "prettier": {
    "singleQuote": true,
    "tabWidth": 2,
    "semi": false,
    "trailingComma": "all",
    "arrowParens": "always",
    "endOfLine": "lf"
  }
}
